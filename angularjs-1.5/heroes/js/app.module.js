
"use strict";

var app = angular.module("myApp", []);

angular.element(function() {
    angular.bootstrap(document, ["myApp"]);
});



/*
Note:
- ng-switch - containing the value of switch.
- ng-switch-when - an option of switch.
- ng-switch-default - the default option of switch.
- ng-transclude - include HTML elements within directive tags
*/
